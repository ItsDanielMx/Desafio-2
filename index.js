const Manager = require('./manager.js')
const manager = new Manager()

let product = {
    name: "Camara 2.0",
    description: "lolololalala",
    price: 350,
    image: "jrehtjrht"
}

// manager.save(product).then(result => console.log(result))

// manager.getAll().then(result => console.log(result))

// manager.getById(1).then(result => console.log(result))

// manager.updateProduct(2, product).then(result => console.log(result))

// manager.deleteById(2).then(result => console.log(result))

// manager.deleteAll().then(result => console.log(result))